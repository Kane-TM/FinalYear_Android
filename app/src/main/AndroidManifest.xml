<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mainactivity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label=""
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DetailCouponActivity"></activity>
        <activity android:name=".CouponActivity" />
        <activity android:name=".SavePointActivity" />
        <activity android:name=".Menu" />
        <activity android:name=".NavigationDrawerActivity" />
        <activity android:name=".UpdateUserPassword" />
        <activity android:name=".FinishRegister" />
        <activity android:name=".UpdateUserInfo" />
        <activity android:name=".UserInfo" />
        <activity android:name=".MainToolBar" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>